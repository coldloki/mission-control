{
  "command": "launchctl load ~/Library/LaunchAgents/com.mission-control.automation.plist && launchctl start com.mission-control.automation",
  "description": "Install and start Mission Control hourly task automation (launchd timer)"
}